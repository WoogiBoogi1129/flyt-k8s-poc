# Build artifacts

Generated guest bundles and PyTorch wheels belong here locally, but are ignored
by Git. Publish release checksums and immutable OCI image digests instead of
committing multi-gigabyte binaries.
